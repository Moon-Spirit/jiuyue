# 07 — 前端壳：响应式布局、认证页、i18n、暗色模式

**What to build:** 路由与布局系统：≥1024px 三栏（导航/会话列表/聊天窗）、640–1024 双栏、<640 单栏栈叠；注册/登录页（邮箱+手机号 Tab 切换、验证码倒计时）接 04 的 HTTP API，token 存内存 + refresh 持久化；vue-i18n zh-CN/en 全量文案 + 语言切换；暗色模式跟随系统 + 手动开关；全局错误/断线提示条。Vitest 冒烟测试。

**Blocked by:** 04-auth-http

**Status:** ready-for-agent

- [ ] Playwright 三视口截图取证：1280×800 三栏 / 900×700 双栏 / 390×844 单栏
- [ ] 注册→登录全流程浏览器实测通过（mock 验证码路径）
- [ ] 中英切换无漏键（i18n key 完整性测试）
- [ ] 明暗主题切换即时生效且持久化偏好
