import type { MessageSchema } from "../index";

const en: MessageSchema = {
  app: {
    name: "JiuYue",
    tagline: "Reliable, cross-platform messaging with optional privacy",
    toggleLocale: "切换到中文",
  },
};

export default en;
