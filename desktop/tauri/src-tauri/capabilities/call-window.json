{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "call-window",
  "description": "A Call window: events and its own window control, and nothing else. In particular it cannot raise notifications.",
  "windows": ["call-*"],
  "permissions": ["core:default"]
}
